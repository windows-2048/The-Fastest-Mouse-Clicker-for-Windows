rem '-c 100' = clicks per second is 100, '-t 32' = trigger key is 32 (space bar), '-s 300' = stop after 300 clicks,
rem '-m t' = clicking mode is toggle, '-b r' = mouse button to click is right
rem click 'Help' button inside the running application for more info

TheFastestMouseClicker.exe -c 100 -t 32 -s 300 -m t -b r

pause
